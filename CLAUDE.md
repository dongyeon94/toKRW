# 작업 규칙

## 웹만 고친다

기능 추가와 수정은 **`web/` 에만** 한다. `android/` 와 `ios/` 는 손대지 않는다.

사용자가 "안드로이드도 반영해줘" 처럼 **명시적으로 말할 때만** 두 폴더를 건드린다.
그 전까지는 웹과 네이티브 앱의 동작이 갈라져도 그대로 둔다. 알아서 맞추지 않는다.

웹을 고쳤다고 해서 안드로이드나 iOS 에 같은 변경을 옮기지 말 것.
"세 벌을 똑같이 유지한다" 는 더 이상 기준이 아니다.

## 지금 갈라진 부분

웹에만 있고 네이티브에는 없는 것들이다. 옮기라는 말이 없었으니 그대로 둔다.

- 통화 즐겨찾기와 숨기기, 기본 프리셋 (동남아·동아시아 11개)
- 사칙연산과 퍼센트 계산기
- 깃허브 액션이 하루 한 번 구워 두는 `web/rates.json` 대비책

네이티브 두 벌은 통화 52개 전체를 보여 주고 계산기가 없다. 이게 정상이다.

## 배포

`web/**` 나 `scripts/fetch-rates.py` 를 고쳐서 `main` 에 올리면
깃허브 액션이 환율을 새로 굽고 깃허브 페이지에 배포한다.
주소는 https://dongyeon94.github.io/toKRW/ 다.

README 나 CLAUDE.md 만 고친 커밋은 배포를 일으키지 않는다. 웹 내용이 안 바뀌었으니 그래도 된다.

액션은 하루 한 번 스스로 돌면서 `web/rates.json` 을 커밋한다.
그래서 푸시하기 전에 `git pull --rebase` 를 먼저 해야 하는 경우가 잦다.

## 검증

웹을 고쳤으면 브라우저 없이 로직을 돌려 보는 테스트 하네스로 확인한다.
`index.html` 안의 스크립트를 최소한의 DOM 대역에 얹어 실제로 실행하는 방식이다.
새 기능을 넣었으면 그에 맞는 항목을 하네스에 추가한다.
